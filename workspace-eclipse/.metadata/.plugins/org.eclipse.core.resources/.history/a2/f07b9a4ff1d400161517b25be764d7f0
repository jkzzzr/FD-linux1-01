package test;

public class Process_uri {

	public static void main(String[] args) {
		String uri = "asdf asdfas-234saf";
		char [] chars = uri.toCharArray();
		char [] newchars = new char[](chars.length);
		for (char c:chars){
			if (Character.isAlphabetic(c)
					||Character.isDigit(c)
					||c=='/'
					||c==':'
					||c=='.'){
				
			}
		}
	}

}
